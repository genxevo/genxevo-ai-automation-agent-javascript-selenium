---
name: Bug report
about: Something the server did that it should not have, or did not do that it should
title: ''
labels: bug
assignees: ''
---

**Do not open a security issue here.** See [SECURITY.md](../../SECURITY.md) for
private reporting.

## What happened

## What you expected

## The result envelope

Paste the whole envelope, with `status`, `error` and `warnings` intact. **Redact
nothing by hand** — if a secret appears in it, that is itself the bug and belongs
in a private security report instead.

```json

```

## `genxevo_agent_status` output

The `data.host`, `data.workspace` and `data.configurationIssues` sections are
usually enough.

```json

```

## Environment

- GenXEvo version:
- Node version (`node --version`):
- OS:
- MCP client:

## The project being scanned

- Test runner:
- Browser automation library:
- Package manager and lockfiles present:

## Reproduction

Smallest project layout that reproduces it. Use synthetic credentials only.
